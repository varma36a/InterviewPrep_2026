"""Dedicated CS Fundamentals section — top 40 interview questions."""

from data.interview_content import InterviewItem, Phase, Section


def _q(id: str, question: str, stub: str = "", points: list[str] | None = None, lang: str = "text") -> InterviewItem:
    return InterviewItem(
        id=id,
        question=question,
        explanation="See detailed explanation.",
        code=stub,
        language=lang,
        key_points=points or [],
    )


CS_SECTION = Section(
    id="cs",
    title="CS Fundamentals",
    emoji="📐",
    color="#7C3AED",
    subtitle="Top 40 computer science basics — OS, networking, DB, concurrency, security",
    phases=[
        Phase("os", "Operating Systems", [
            _q("cs-process-vs-thread", "Process vs thread — key differences?",
               "Process: own memory space | Thread: shared memory within process",
               ["Context switch cost", "IPC vs shared memory", "Multi-threading for I/O"]),
            _q("cs-deadlock", "Deadlock — necessary conditions and prevention",
               "Mutual exclusion, hold & wait, no preemption, circular wait",
               ["Banker's algorithm", "Lock ordering", "Try-lock timeout"]),
            _q("cs-virtual-memory", "Virtual memory and paging — how it works",
               "Logical address → page table → physical frame; page faults",
               ["TLB cache", "Swapping", "Demand paging"]),
            _q("cs-context-switch", "What is context switching and why is it expensive?",
               "Save/restore CPU registers, PC, stack; kernel scheduler",
               ["More threads ≠ always faster", "User vs kernel mode"]),
            _q("cs-cpu-scheduling", "CPU scheduling algorithms — FCFS, SJF, Round Robin",
               "RR: time quantum for fairness | SJF: minimize wait (can starve)",
               ["Preemptive vs non-preemptive", "Multi-level feedback queue"]),
        ]),
        Phase("networking", "Computer Networking", [
            _q("cs-osi-tcpip", "OSI model vs TCP/IP model — layers explained",
               "OSI 7 layers | TCP/IP 4 layers: Link, Internet, Transport, Application",
               ["HTTP at application", "TCP/UDP at transport", "IP at network"]),
            _q("cs-tcp-vs-udp", "TCP vs UDP — when to use each?",
               "TCP: reliable, ordered, connection | UDP: fast, loss-tolerant",
               ["Video streaming often UDP", "HTTP/3 over QUIC", "DNS uses UDP"]),
            _q("cs-http-versions", "HTTP/1.1 vs HTTP/2 vs HTTP/3 — differences",
               "H2: multiplexing, header compression | H3: QUIC over UDP",
               ["Head-of-line blocking fixed in H2/H3", "Server push (H2)"]),
            _q("cs-tls-handshake", "How does TLS/HTTPS handshake work?",
               "ClientHello → ServerHello + cert → key exchange → encrypted records",
               ["Certificate chain validation", "Forward secrecy", "TLS 1.3 fewer round trips"]),
            _q("cs-dns", "DNS resolution — step by step",
               "Browser cache → OS → resolver → root → TLD → authoritative",
               ["TTL caching", "A vs CNAME vs MX", "DNS over HTTPS"]),
            _q("cs-tcp-handshake", "TCP three-way handshake and four-way teardown",
               "SYN → SYN-ACK → ACK | FIN-WAIT, TIME-WAIT states",
               ["Why not two-way?", "TIME-WAIT 2×MSL"]),
        ]),
        Phase("databases", "Databases & Storage", [
            _q("cs-acid", "ACID properties — explain each",
               "Atomicity, Consistency, Isolation, Durability",
               ["Transactions", "Write-ahead log", "Isolation level trade-offs"]),
            _q("cs-normalization", "Database normalization — 1NF, 2NF, 3NF",
               "Eliminate redundancy; 1NF atomic cols; 2NF no partial deps; 3NF no transitive",
               ["Denormalize for read perf", "BCNF stricter"]),
            _q("cs-db-indexing", "How do database indexes work? B-tree vs hash",
               "B-tree: range queries, sorted | Hash: equality O(1) avg",
               ["Clustered vs non-clustered", "Covering index", "Write amplification"]),
            _q("cs-isolation-levels", "Transaction isolation levels — dirty read to serializable",
               "Read Uncommitted → Read Committed → Repeatable Read → Serializable",
               ["Phantom reads", "SQL Server default RC", "PostgreSQL RR = snapshot"]),
        ]),
        Phase("memory-runtime", "Memory & Runtime", [
            _q("cs-stack-vs-heap", "Stack vs heap memory — differences",
               "Stack: LIFO, locals, fast | Heap: dynamic alloc, GC managed in .NET",
               ["Value types often stack", "Reference types on heap", "Stack overflow"]),
            _q("cs-garbage-collection", "How does garbage collection work (.NET / JVM)?",
               "Mark-and-sweep, generational GC (Gen0/1/2), compact heap",
               ["Stop-the-world pauses", "IDisposable for unmanaged", "LOH large objects"]),
            _q("cs-compiler-interpreter", "Compiler vs interpreter — difference",
               "Compiler: source → machine code ahead of time | Interpreter: line-by-line",
               ["JIT hybrid (.NET CLR)", "Bytecode (IL)", "AOT in Native AOT"]),
            _q("cs-cpu-cache", "CPU cache L1/L2/L3 — why it matters for performance",
               "Cache locality; false sharing; array-of-structs vs struct-of-arrays",
               ["Cache line 64 bytes", "Sequential access faster", "Prefetching"]),
        ]),
        Phase("algorithms", "Algorithms & Data Structures", [
            _q("cs-big-o", "Big O notation — common time complexities",
               "O(1) < O(log n) < O(n) < O(n log n) < O(n²) < O(2^n)",
               ["Amortized analysis", "Space complexity", "Best vs worst vs average"]),
            _q("cs-hash-table-internals", "How does a hash table work internally?",
               "Hash function → bucket; collision: chaining or open addressing",
               ["Load factor rehash", "Dictionary in .NET", "Consistent hashing at scale"]),
            _q("cs-sorting-comparison", "Compare sorting algorithms — QuickSort, MergeSort, HeapSort",
               "MergeSort stable O(n log n) | QuickSort avg O(n log n), in-place",
               ["TimSort hybrid", "When n small → insertion sort"]),
        ]),
        Phase("concurrency", "Concurrency & Parallelism", [
            _q("cs-mutex-semaphore", "Mutex vs semaphore vs monitor",
               "Mutex: one owner | Semaphore: count N | Monitor: lock + wait/pulse",
               ["lock() in C#", "SemaphoreSlim", "Deadlock risk"]),
            _q("cs-race-condition", "Race condition — what and how to prevent?",
               "Outcome depends on thread interleaving; use locks, atomic, immutability",
               ["Interlocked.Increment", "Thread-safe collections", "Avoid shared mutable state"]),
            _q("cs-thread-pool", "Thread pool — why and how (.NET ThreadPool)?",
               "Reuse threads; avoid create/destroy cost; queue work items",
               ["Task.Run uses pool", "Starvation", "ConfigureAwait in library code"]),
        ]),
        Phase("security", "Security Fundamentals", [
            _q("cs-encryption-types", "Symmetric vs asymmetric encryption",
               "Symmetric: AES shared key fast | Asymmetric: RSA/EC key pair for exchange",
               ["HTTPS uses both", "Digital signatures", "Key rotation"]),
            _q("cs-oauth-flow", "OAuth 2.0 authorization code flow — explain",
               "Redirect to IdP → auth code → exchange for access + refresh token",
               ["PKCE for SPAs", "Never expose client secret in browser", "Scopes"]),
            _q("cs-jwt-structure", "JWT structure — header, payload, signature",
               "Base64url encoded; HMAC or RSA sign; stateless auth",
               ["Validate iss/aud/exp", "Don't put secrets in payload", "Short expiry + refresh"]),
            _q("cs-owasp-top", "SQL injection, XSS, CSRF — explain and prevent",
               "SQLi: parameterized queries | XSS: encode output | CSRF: anti-forgery tokens",
               ["CSP headers", "HttpOnly cookies", "SameSite cookies"]),
        ]),
        Phase("architecture", "Architecture, Cloud & Distributed", [
            _q("cs-rest-vs-grpc", "REST vs gRPC — trade-offs",
               "REST: HTTP/JSON human-friendly | gRPC: HTTP/2 Protobuf binary fast",
               ["Streaming in gRPC", "Browser gRPC-Web", "OpenAPI for REST"]),
            _q("cs-pub-sub", "Pub/Sub messaging pattern",
               "Publishers → topic → subscribers decoupled; at-least-once common",
               ["Kafka log retention", "Event vs command", "Fan-out"]),
            _q("cs-cap-theorem", "CAP theorem — consistency, availability, partition tolerance",
               "During partition choose C or A; real systems are AP or CP with tunable consistency",
               ["BASE vs ACID", "Eventual consistency examples"]),
            _q("cs-consistent-hashing", "Consistent hashing — use cases",
               "Distributed cache, sharding, load balancing with minimal remapping",
               ["Virtual nodes", "Hash ring"]),
            _q("cs-load-balancing-algos", "Load balancing algorithms",
               "Round robin, weighted, least connections, IP hash, random",
               ["Health checks remove bad nodes", "Sticky sessions trade-off"]),
            _q("cs-client-server-p2p", "Client-server vs peer-to-peer architecture",
               "Client-server: centralized control | P2P: BitTorrent, blockchain nodes",
               ["Scalability vs consistency", "Hybrid CDN edge"]),
            _q("cs-cqrs", "What is CQRS?",
               "Command Query Responsibility Segregation — separate read/write models",
               ["Event sourcing optional", "Read replicas optimized", "Not for every app"]),
            _q("cs-iaas-paas-saas", "IaaS vs PaaS vs SaaS — examples",
               "IaaS: VMs (Azure VM) | PaaS: App Service | SaaS: Office 365",
               ["Shared responsibility model", "PaaS faster deploy"]),
            _q("cs-scalability-elasticity", "Scalability vs elasticity in cloud",
               "Scalability: handle growth | Elasticity: auto scale up/down on demand",
               ["Horizontal pod autoscaler", "Cost vs performance"]),
        ]),
        Phase("sdlc", "SDLC, CI/CD & Testing", [
            _q("cs-cicd-pipeline", "CI/CD pipeline — typical stages",
               "Source → Build → Test → Deploy → Monitor; trunk-based or GitFlow",
               ["Blue-green / canary deploy", "Feature flags", "Rollback strategy"]),
            _q("cs-testing-pyramid", "Testing pyramid and test types",
               "Many unit tests, fewer integration, few E2E; plus contract/load tests",
               ["Testcontainers for integration", "Shift-left testing", "TDD optional"]),
        ]),
    ],
)


def apply_cs_section(sections: dict, detailed: dict) -> None:
    from data.cs_fundamentals_detailed import CS_DETAILED

    sections["cs"] = CS_SECTION
    detailed.update(CS_DETAILED)
